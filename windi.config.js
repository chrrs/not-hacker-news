module.exports = {
    extract: {
        include: ['**/*.{tsx,css}'],
        exclude: ['node_modules', '.git', '.next/**/*'],
    },
    darkMode: false,
    theme: {
        extend: {},
    },
    plugins: [],
};
